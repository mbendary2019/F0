# 🚀 دليل التشغيل السريع - From Zero

## 📋 جدول المحتويات
1. [التحقق من المتطلبات](#التحقق-من-المتطلبات)
2. [تشغيل المحاكيات محليًا](#تشغيل-المحاكيات-محليًا)
3. [بذر البيانات الأولية](#بذر-البيانات-الأولية)
4. [تشغيل التطبيق](#تشغيل-التطبيق)
5. [النشر على الإنتاج](#النشر-على-الإنتاج)
6. [حل المشاكل الشائعة](#حل-المشاكل-الشائعة)

---

## ✅ التحقق من المتطلبات

```bash
# تحقق من الإصدارات
node -v        # يجب أن يكون v22.x ✅
pnpm -v        # موجود ✅
firebase --version  # Firebase CLI ✅
```

**النتائج المتوقعة:**
- Node.js: v22.17.1 ✅
- pnpm: 10.18.0 ✅
- Firebase CLI: 14.15.2 ✅

---

## 🔧 إعداد المتغيرات البيئية

### 1. للتطوير المحلي (`.env.local`)

```bash
# انسخ القالب
cp .env.local.example .env.local

# عدّل .env.local وأضف:
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123

# Stripe (اختياري للتطوير)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
```

### 2. للـ Functions (`functions/.env`)

```bash
cd functions
cat > .env << EOF
FIREBASE_PROJECT_ID=your-project-id
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
APP_URL=http://localhost:3000
EOF
```

---

## 🏃 تشغيل المحاكيات محليًا

### الطريقة 1: استخدام سكريبت pnpm (موصى به)

```bash
# إذا كان لديك سكريبت emu في package.json
pnpm -w emu
```

### الطريقة 2: استخدام Firebase CLI مباشرة

```bash
# تشغيل المحاكيات الأساسية
firebase emulators:start --only functions,firestore,auth

# أو مع Hosting للاختبار الكامل
firebase emulators:start
```

**ملاحظات:**
- Firestore Emulator: `http://localhost:8080`
- Auth Emulator: `http://localhost:9099`
- Functions Emulator: `http://localhost:5001`
- Hosting Emulator: `http://localhost:5000`
- Emulator UI: معطّل (يمكن تفعيله من `firebase.json`)

### تفعيل Emulator UI (اختياري)

```bash
# عدّل firebase.json
# غيّر "enabled": false إلى "enabled": true في قسم emulators.ui

# ثم أعد التشغيل
firebase emulators:start
# الآن يمكنك فتح: http://localhost:4000
```

---

## 🌱 بذر البيانات الأولية

### السكريبت الشامل (موصى به)

```bash
# للمحاكي المحلي
FIRESTORE_EMULATOR_HOST=localhost:8080 node scripts/seed-all.js

# للإنتاج
FIREBASE_SERVICE_ACCOUNT_FILE=/path/to/service-account.json node scripts/seed-all.js

# أو إذا كنت مسجل دخول عبر Firebase CLI
node scripts/seed-all.js
```

### السكريبتات المنفصلة

```bash
# خطط الفوترة + المتجر المدفوع
node scripts/seed-phase45.js

# عناصر السوق المدفوعة
node scripts/seed-marketplace-paid.js

# بيانات تجريبية للتطوير
node scripts/seed-phase47-demo.js
```

### البيانات التي سيتم إضافتها

| Collection | المستندات | الوصف |
|-----------|----------|--------|
| `ops_branding` | 1 | إعدادات الهوية البصرية |
| `ops_billing_plans` | 3 | خطط: Trial, Starter, Pro |
| `ops_marketplace_items` | 3 | عناصر مجانية |
| `ops_marketplace_paid` | 2 | عناصر احترافية |
| `ops_system_settings` | 1 | إعدادات النظام |

### التحقق من البيانات

```bash
# عبر Firebase CLI
firebase firestore:get ops_billing_plans/pro

# أو افتح Firestore Console
open "https://console.firebase.google.com/project/YOUR_PROJECT/firestore"
```

---

## 🚀 تشغيل التطبيق

### 1. التطبيق الرئيسي (Next.js)

```bash
# في terminal جديد
pnpm dev

# أو للويب فقط
pnpm dev:web

# سيعمل على: http://localhost:3000
```

### 2. تطبيق سطح المكتب (Electron - اختياري)

```bash
# في terminal آخر
pnpm dev:desktop
```

### 3. الأوركستريتور (اختياري)

```bash
cd orchestrator
pnpm install
pnpm dev

# سيعمل على: http://localhost:9090
```

### التشغيل الكامل (كل شيء معًا)

```bash
# في terminal واحد - كل الخدمات
pnpm dev:all
```

---

## 🔥 النشر على الإنتاج

### التحضير

```bash
# 1. تحديث متغيرات البيئة
# عدّل .env.production

# 2. Build التطبيق
pnpm build

# 3. Build Functions
cd functions
npm run build
cd ..
```

### النشر الكامل

```bash
# نشر كل شيء (Hosting + Functions)
firebase deploy

# أو نشر Functions فقط
firebase deploy --only functions

# أو نشر Hosting فقط
firebase deploy --only hosting

# أو نشر Firestore Rules
firebase deploy --only firestore:rules
```

### النشر المرحلي (موصى به للإنتاج)

```bash
# المرحلة 1: Firestore Rules و Indexes
firebase deploy --only firestore

# المرحلة 2: Functions الحساسة أولاً
firebase deploy --only functions:handleStripeWebhook,functions:createCheckoutSession

# المرحلة 3: باقي Functions
firebase deploy --only functions

# المرحلة 4: Hosting
firebase deploy --only hosting
```

### التحقق من النشر

```bash
# اختبار Health Check
curl https://YOUR_PROJECT.web.app/api/health

# عرض Logs
firebase functions:log --only handleStripeWebhook

# أو عبر gcloud
gcloud logging read "resource.type=cloud_function" --limit 50 --format json
```

---

## 🧪 اختبارات الدخان السريعة

### السكريبت الجاهز

```bash
# إذا كان موجود
pnpm -w test:smoke

# أو
./scripts/smoke-tests.sh
```

### اختبار يدوي سريع

```bash
# 1. Health Check
curl -s http://localhost:3000/api/health
# النتيجة المتوقعة: {"status":"ok"}

# 2. Orchestrator Readiness
curl -s http://localhost:9090/readyz
# النتيجة المتوقعة: {"status":"ready"}

# 3. Firebase Auth
# افتح المتصفح وسجل دخول
open http://localhost:3000/auth/signin

# 4. Test Stripe Webhook (محليًا)
stripe listen --forward-to localhost:9090/webhook/stripe
# في terminal آخر:
stripe trigger payment_intent.succeeded
```

---

## 🛠️ حل المشاكل الشائعة

### ❌ مشكلة: `Missing NEXT_PUBLIC_*` في المتصفح

**الحل:**
```bash
# 1. تأكد من وجود .env.local
ls -la .env.local

# 2. تأكد من أن المتغيرات تبدأ بـ NEXT_PUBLIC_
cat .env.local | grep NEXT_PUBLIC

# 3. أعد تشغيل dev server
killall node
pnpm dev
```

### ❌ مشكلة: `Unauthorized` في `/api/last`

**الأسباب المحتملة:**
1. الأوركستريتور غير مشغّل
2. `FIREBASE_PROJECT_ID` غير صحيح في `orchestrator/.env`
3. Service Account غير موجود

**الحل:**
```bash
# 1. تحقق من الأوركستريتور
curl http://localhost:9090/readyz

# 2. إذا لم يكن يعمل، شغّله
cd orchestrator
cat .env  # تحقق من FIREBASE_PROJECT_ID
pnpm dev

# 3. تحقق من Service Account
ls -la ~/.secrets/firebase.json
```

### ❌ مشكلة: `EADDRINUSE - البورت محجوز`

**الحل:**
```bash
# اقتل جميع عمليات Node
killall node

# أو اقتل البورت المحدد
lsof -ti:3000 | xargs kill -9
lsof -ti:9090 | xargs kill -9

# أعد التشغيل
pnpm dev
```

### ❌ مشكلة: `npm ci` خطأ في lockfile

**الحل:**
```bash
# المشروع يستخدم pnpm وليس npm
# لا تستخدم npm أبدًا!

# استخدم فقط:
pnpm install
```

### ❌ مشكلة: "مش ظاهر حاجة في الموقع"

**الأسباب:**
1. بيانات Firestore ناقصة
2. Feature flags مقفولة

**الحل:**
```bash
# 1. تحقق من البيانات
firebase firestore:get ops_billing_plans/pro

# 2. إذا كانت فارغة، ابذر البيانات
FIRESTORE_EMULATOR_HOST=localhost:8080 node scripts/seed-all.js

# 3. تحقق من System Settings
firebase firestore:get ops_system_settings/global

# 4. أعد تحميل الصفحة
# Ctrl+Shift+R (hard reload)
```

### ❌ مشكلة: Functions لا تعمل محليًا

**الحل:**
```bash
# 1. تأكد من build الـ Functions
cd functions
npm run build

# 2. تحقق من .env
cat .env

# 3. أعد تشغيل Emulators
cd ..
firebase emulators:start --only functions,firestore,auth

# 4. اختبر Function محددة
curl -X POST http://localhost:5001/YOUR_PROJECT/us-central1/yourFunction \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

---

## 📊 مراقبة الـ Scheduled Functions

### عرض الـ Jobs المجدولة

```bash
# في الإنتاج
gcloud scheduler jobs list --project=YOUR_PROJECT

# عرض تفاصيل job محددة
gcloud scheduler jobs describe aggregateDailyMetrics --location=us-central1

# تشغيل job يدويًا
gcloud scheduler jobs run aggregateDailyMetrics --location=us-central1
```

### محليًا (Emulators)

```bash
# المحاكي لا يدعم cron بشكل كامل
# للاختبار، استدعِ الـ Function مباشرة:

# مثال: استدعاء aggregateDailyMetrics
curl -X POST \
  http://localhost:5001/YOUR_PROJECT/us-central1/aggregateDailyMetrics \
  -H "Content-Type: application/json"
```

---

## 🔍 أوامر مفيدة إضافية

### Firebase

```bash
# تسجيل الدخول
firebase login

# تحديد المشروع
firebase use YOUR_PROJECT

# عرض المشاريع المتاحة
firebase projects:list

# عرض Functions المنشورة
firebase functions:list

# عرض Logs آخر ساعة
firebase functions:log --only handleStripeWebhook --since 1h
```

### Firestore

```bash
# استيراد بيانات
firebase firestore:import ./backup

# تصدير بيانات
firebase firestore:export ./backup

# حذف collection (حذر!)
firebase firestore:delete --all-collections --force
```

### Stripe (للاختبار المحلي)

```bash
# تثبيت Stripe CLI
brew install stripe/stripe-cli/stripe

# تسجيل الدخول
stripe login

# الاستماع للـ webhooks
stripe listen --forward-to localhost:9090/webhook/stripe

# محاكاة حدث
stripe trigger payment_intent.succeeded
stripe trigger customer.subscription.created
```

---

## 📚 مصادر إضافية

- [Firebase Documentation](https://firebase.google.com/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [Stripe Documentation](https://stripe.com/docs)
- [PHASE_48_COMPLETE.md](./PHASE_48_COMPLETE.md) - آخر مرحلة منفذة
- [QUICK_START.md](./QUICK_START.md) - دليل البداية السريعة

---

## 🆘 الدعم

إذا واجهت أي مشاكل:

1. ✅ راجع قسم [حل المشاكل الشائعة](#حل-المشاكل-الشائعة)
2. 📖 افحص الـ logs: `firebase functions:log`
3. 🔍 تحقق من Firestore Console
4. 💬 افتح issue في GitHub
5. 📧 اتصل بالدعم: support@fromzero.app

---

**آخر تحديث:** 2025-10-14
**الإصدار:** 1.0.0
**الحالة:** ✅ جاهز للإنتاج
