# F0 — ملخص التنفيذ: خطة الاستقرار 7 أيام (المرحلة 49)

**التاريخ:** 2025-11-05
**الحالة:** ✅ اكتمل تنفيذ المهام الأساسية (اليوم 1، 4، 5)
**المرحلة:** 49 - استقرار Firebase + تتبع الأخطاء وإدارة الحوادث

---

## 📊 نظرة عامة

تم تنفيذ المهام الأساسية من خطة الاستقرار التي تستغرق 7 أيام، مع التركيز على:
- ✅ توحيد ملفات تهيئة Firebase
- ✅ توحيد تكوين البيئة (.env)
- ✅ تجميع المقاييس اليومية
- ✅ تصدير الحوادث إلى CSV
- ✅ التحقق من المحاكيات (Emulators)

---

## ✅ المهام المكتملة

### اليوم 1: توحيد Firebase وإعداد البيئة

#### 1. حل مشكلة التهيئة المكررة ✅

**المشكلة:** وجود ملفات متعددة لتهيئة Firebase تسبب تعارضات

**الحل:** توحيد في ملفين رئيسيين:

##### العميل (المتصفح): `src/lib/firebaseClient.ts`
- ✅ نمط Singleton لمنع التهيئة المكررة
- ✅ تكامل App Check مع ReCaptcha Enterprise
- ✅ الاتصال بالمحاكيات (Firestore, Auth, Functions)
- ✅ معالجة الأخطاء بشكل صحيح
- ✅ حماية لضمان التشغيل في المتصفح فقط

##### الخادم (Server): `src/lib/firebase-admin.ts`
- ✅ تهيئة Firebase Admin SDK
- ✅ دعم Application Default Credentials
- ✅ تجاوز التحقق في وضع المحاكي
- ✅ دوال مساعدة للتحقق من الرموز
- ✅ التحقق من Session Cookies

**الملفات المحذوفة:**
- ❌ `src/lib/firebase.ts` (مكرر - تم دمجه في firebaseClient.ts)

#### 2. قالب البيئة الشامل ✅

**تم إنشاء:** `.env.local.example`

يحتوي على أقسام لـ:
- ✅ إعدادات F0 SDK
- ✅ إعدادات Firebase للعميل
- ✅ إعدادات محاكيات Firebase
- ✅ App Check (ReCaptcha Enterprise)
- ✅ مفاتيح Web Push (FCM VAPID)
- ✅ Sentry لتتبع الأخطاء
- ✅ Stripe للمدفوعات
- ✅ OpenAI API
- ✅ نقاط نهاية تتبع الأخطاء (المرحلة 49)

**ملف `.env.local` الحالي:** ✅ جميع المفاتيح محددة ومضبوطة

#### 3. سكريبت التحقق من المحاكيات ✅

**تم إنشاء:** `scripts/verify-emulators.sh`

**المميزات:**
- ✅ فحص توفر المنافذ (4000, 9099, 8080, 5001, 9199)
- ✅ التحقق من نقاط نهاية HTTP
- ✅ مخرجات ملونة للحالة
- ✅ إرشادات الخطوات التالية

**الاستخدام:**
```bash
chmod +x scripts/verify-emulators.sh
./scripts/verify-emulators.sh
```

---

### اليوم 4: جدولة التجميع اليومي + TTL

#### 1. دالة تجميع المقاييس اليومية ✅

**الملف:** `functions/src/analytics/aggregateDailyMetrics.ts`

**ملاحظة:** هذه الدالة موجودة بالفعل ومضبوطة بشكل صحيح.

**المميزات:**
- ✅ تشغيل مجدول الساعة 02:10 (توقيت الكويت)
- ✅ تجميع ops_events إلى مقاييس يومية
- ✅ تتبع DAU، الرموز، الطلبات، المقاعد
- ✅ الكتابة إلى `ops_metrics_daily`
- ✅ تسجيل الأخطاء في ops_incidents

**الجدول الزمني:** `every day 02:10` (توقيت الكويت)

**المجموعات:**
- **الإدخال:** `ops_events` (مفلترة حسب الوقت)
- **الإخراج:** `ops_metrics_daily/{YYYY-MM-DD}`

**المقاييس المتتبعة:**
- المستخدمون النشطون يوميًا (DAU)
- إجمالي الرموز المستهلكة
- طلبات API
- المقاعد المستخدمة
- المنظمات النشطة

#### 2. إعداد TTL (وقت الحياة) ✅

**المجموعة:** `ops_events`
**حقل TTL:** `expire` (Timestamp)
**الاحتفاظ:** 30 يومًا (موصى به)

**تعليمات الإعداد:**
1. انتقل إلى Firestore console
2. اختر مجموعة `ops_events`
3. فعّل سياسة TTL على حقل `expire`
4. حدد فترة الاحتفاظ: 30 يومًا

**عند إنشاء الأحداث:**
```typescript
await db.collection('ops_events').add({
  // ... بيانات الحدث
  expire: Timestamp.fromMillis(Date.now() + 30 * 24 * 60 * 60 * 1000), // 30 يوم
});
```

---

### اليوم 5: مركز الحوادث (المرحلة 49)

#### 1. دالة تصدير الحوادث إلى CSV ✅

**الملف:** `functions/src/exportIncidentsCsv.ts`

**نقاط النهاية:**
1. **نقطة HTTP:** `exportIncidentsCsv`
   - الرابط: `https://us-central1-{project-id}.cloudfunctions.net/exportIncidentsCsv`
   - الطريقة: GET
   - المصادقة: عامة (أضف middleware للمصادقة إذا لزم الأمر)

2. **دالة قابلة للاستدعاء:** `exportIncidentsCsvCallable`
   - تتطلب مصادقة
   - تتطلب دور المسؤول
   - ترجع بيانات CSV في الاستجابة

**معاملات الاستعلام:**
- `dateFrom` (string): تاريخ البدء (YYYY-MM-DD)
- `dateTo` (string): تاريخ النهاية (YYYY-MM-DD)
- `level` (string): تصفية حسب المستوى (error|warning|info)
- `status` (string): تصفية حسب الحالة (open|ack|resolved)
- `limit` (number): أقصى عدد من النتائج (افتراضي: 1000)

**مثال الاستخدام:**
```bash
# نقطة HTTP
curl "https://us-central1-from-zero-84253.cloudfunctions.net/exportIncidentsCsv?dateFrom=2025-11-01&dateTo=2025-11-05&level=error" -o incidents.csv

# من العميل
import { getFunctions, httpsCallable } from 'firebase/functions';
const functions = getFunctions();
const exportCsv = httpsCallable(functions, 'exportIncidentsCsvCallable');
const result = await exportCsv({
  dateFrom: '2025-11-01',
  dateTo: '2025-11-05',
  level: 'error',
  status: 'open'
});
```

**أعمدة CSV:**
- ID (معرف المستند)
- Created At (وقت الإنشاء)
- Updated At (وقت التحديث)
- Source (المصدر)
- Level (المستوى)
- Status (الحالة)
- Message (الرسالة)
- Stack Trace (تتبع المكدس)
- Context (السياق JSON)

#### 2. مخطط ops_incidents ✅

**المجموعة:** `ops_incidents`

**بنية المستند:**
```typescript
{
  source: 'client' | 'functions' | 'manual',
  level: 'error' | 'warning' | 'info',
  message: string,
  stack?: string,
  context: object,
  status: 'open' | 'ack' | 'resolved',
  createdAt: Timestamp,
  updatedAt: Timestamp,
}
```

**فهارس Firestore المطلوبة:**
```
ops_incidents
  - level ASC, createdAt DESC
  - status ASC, createdAt DESC
  - createdAt DESC
```

---

## 🎯 الملفات المحدثة

### 1. عميل Firebase
- **الملف:** `src/lib/firebaseClient.ts`
- **الحالة:** ✅ موحد ومحسّن
- **المميزات:** Singleton، App Check، دعم المحاكي

### 2. Firebase Admin
- **الملف:** `src/lib/firebase-admin.ts`
- **الحالة:** ✅ موحد مع دوال مساعدة
- **المميزات:** دعم ADC، التحقق من الرموز

### 3. إعدادات Functions
- **الملف:** `functions/src/config.ts`
- **الحالة:** ✅ تمت إضافة تصدير Firestore
- **التغيير:** أضيف `export const db = admin.firestore();`

### 4. قالب البيئة
- **الملف:** `.env.local.example`
- **الحالة:** ✅ قالب شامل تم إنشاؤه
- **الأقسام:** أكثر من 10 أقسام تكوين

### 5. تصدير Functions
- **الملف:** `functions/src/index.ts`
- **الحالة:** ✅ تمت إضافة exportIncidentsCsv
- **القسم:** Phase 49

---

## 📋 المهام المتبقية (الأيام 2-7)

### اليوم 2: واجهة Auth وApp Check
- [ ] تفعيل Email/Google في المحاكي
- [ ] اختبار تدفق التسجيل/الدخول محليًا
- [ ] التحقق من تكامل App Check

### اليوم 3: الثيم والتخطيط
- [ ] تثبيت Neon theme
- [ ] تنفيذ دعم RTL/LTR ديناميكي
- [ ] توحيد مكون Sidebar
- [ ] إنشاء لقطات قبل/بعد

### اليوم 6: سجل التدقيق ولوحة Ops
- [ ] مراجعة تدفق ops_audit
- [ ] اختبار البحث/التصفية
- [ ] التحقق من لوحة `/ops/analytics`
- [ ] التحقق من لوحة `/ops/audit`
- [ ] فحص أداء فهارس Firestore

### اليوم 7: الاستقرار النهائي وGo/No-Go
- [ ] تنفيذ قوائم التحقق الشاملة
- [ ] التقاط لقطات/سجلات
- [ ] تنظيف TODOs وتعليقات الكود
- [ ] تحديث أرقام الإصدارات
- [ ] إنشاء تقرير الجاهزية النهائي
- [ ] إغلاق المرحلة 49

---

## 🚀 دليل النشر السريع

### التحقق من المتطلبات
```bash
# 1. تثبيت التبعيات
npm install
cd functions && npm install && cd ..

# 2. نسخ قالب البيئة
cp .env.local.example .env.local
# تحرير .env.local بمعلومات مشروعك

# 3. بدء المحاكيات
firebase emulators:start --only firestore,auth,functions,storage,ui

# 4. التحقق من المحاكيات (في نافذة جديدة)
./scripts/verify-emulators.sh

# 5. بدء خادم التطوير
npm run dev
```

### نشر Functions
```bash
# بناء functions
cd functions
npm run build

# نشر دوال محددة
firebase deploy --only functions:exportIncidentsCsv
firebase deploy --only functions:exportIncidentsCsvCallable

# نشر جميع دوال المرحلة 49
firebase deploy --only functions
```

---

## 🧪 دليل الاختبار السريع

### 1. اختبار تصدير CSV

```bash
# محلي (محاكي)
curl "http://127.0.0.1:5001/from-zero-84253/us-central1/exportIncidentsCsv?limit=10" -o test.csv

# إنتاج
curl "https://us-central1-from-zero-84253.cloudfunctions.net/exportIncidentsCsv?limit=10" -o incidents.csv
```

### 2. اختبار تجميع المقاييس

```bash
# تشغيل التجميع يدويًا
firebase functions:call aggregateDailyMetrics_manual --data='{"date":"2025-11-05"}'

# التحقق من النتائج في Firestore
# المجموعة: ops_metrics_daily
# المستند: 2025-11-05
```

### 3. إنشاء حادث تجريبي

في Emulator UI → Firestore → إضافة مستند:

```json
المجموعة: ops_incidents
معرف المستند: (تلقائي)

البيانات:
{
  "source": "manual",
  "level": "error",
  "message": "حادث تجريبي لتصدير CSV",
  "stack": "Error: خطأ تجريبي\n  في testFunction",
  "context": {
    "test": true,
    "timestamp": "2025-11-05T10:00:00Z"
  },
  "status": "open",
  "createdAt": "2025-11-05T10:00:00Z",
  "updatedAt": "2025-11-05T10:00:00Z"
}
```

---

## 🔐 اعتبارات الأمان

### 1. مصادقة تصدير CSV
حاليًا، نقطة HTTP عامة. فكر في إضافة:
- التحقق من رمز Firebase Auth
- فحص دور المسؤول
- تحديد المعدل (Rate limiting)
- تكوين CORS

### 2. الكشف عن وضع المحاكي
- تم تجاوز التحقق من الرمز في وضع المحاكي
- تم تعطيل App Check في وضع المحاكي
- تأكد من `NEXT_PUBLIC_USE_EMULATORS=1` فقط في التطوير

### 3. متغيرات البيئة
- لا تُرسل `.env.local` إلى version control
- استخدم `.env.local.example` كقالب
- قم بتدوير مفاتيح API بانتظام
- استخدم Firebase secrets للإنتاج

---

## 📊 مقاييس النجاح

### تقني ✅
- ✅ صفر تهيئات مكررة لـ Firebase
- ✅ مصدر واحد للحقيقة لإعدادات Firebase
- ✅ التحقق من اتصال المحاكي
- ✅ نشر Functions بنجاح
- ✅ عمل تصدير CSV

### تشغيلي ⏳
- ⏳ تشغيل تجميع المقاييس اليومي
- ⏳ سياسة TTL نشطة على ops_events
- ⏳ تتبع الحوادث تشغيلي
- ⏳ استخدام تصديرات CSV من قبل الفريق

---

## 📚 مراجع الملفات الرئيسية

| الملف | الغرض |
|------|--------|
| `src/lib/firebaseClient.ts` | Firebase SDK للعميل |
| `src/lib/firebase-admin.ts` | Firebase Admin للخادم |
| `functions/src/exportIncidentsCsv.ts` | دالة تصدير CSV |
| `functions/src/analytics/aggregateDailyMetrics.ts` | تجميع المقاييس |
| `.env.local.example` | قالب البيئة |
| `scripts/verify-emulators.sh` | التحقق من المحاكيات |
| `PHASE_49_7DAY_STABILIZATION.md` | الدليل الكامل (إنجليزي) |
| `QUICK_START_PHASE49.md` | دليل البدء السريع (إنجليزي) |

---

## 📝 تعريف الإنجاز (Definition of Done)

### تهيئة Firebase ✅
- [x] لا توجد استدعاءات مكررة لـ `initializeApp`
- [x] يستخدم العميل نمط singleton
- [x] تهيئة Admin مرة واحدة
- [x] معالجة صحيحة للأخطاء

### تكوين البيئة ✅
- [x] جميع المفاتيح موثقة في .env.local.example
- [x] تكوين المحاكي
- [x] تكوين App Check
- [x] مفاتيح VAPID لـ Web Push
- [x] Sentry DSN

### دعم المحاكي ✅
- [x] تم إنشاء سكريبت التحقق
- [x] تم تنفيذ فحوصات المنافذ
- [x] التحقق من نقاط نهاية HTTP
- [x] توثيق الاستخدام

### التحليلات والمقاييس ✅
- [x] دالة التجميع اليومي موجودة
- [x] تكوين التنفيذ المجدول
- [x] توثيق إعداد TTL
- [x] تسجيل الأخطاء في الحوادث

### إدارة الحوادث ✅
- [x] نقطة HTTP لتصدير CSV
- [x] دالة قابلة للاستدعاء لتصدير CSV
- [x] دعم تصفية الاستعلامات
- [x] تعريف مخطط ops_incidents
- [x] توثيق فهارس Firestore

---

## 🎉 الخلاصة

تم إنجاز المهام الأساسية بنجاح:
- ✅ توحيد Firebase وحل مشكلة التهيئة المكررة
- ✅ قالب بيئة شامل للتطوير والإنتاج
- ✅ سكريبت التحقق من المحاكيات
- ✅ دوال تجميع المقاييس اليومية
- ✅ تصدير الحوادث إلى CSV
- ✅ توثيق كامل بالعربية والإنجليزية

**الحالة:** جاهز لتنفيذ الأيام 2-7 ✅

**آخر تحديث:** 2025-11-05
