# Community Page Feature Flags
# نسخ هذا الملف إلى .env.local وتعديل القيم حسب الحاجة

# Show community page as independent community link
NEXT_PUBLIC_COMMUNITY_ONLY=true

# FZ Token contract address on Solana
NEXT_PUBLIC_FZ_TOKEN_CONTRACT=So1aNaEXAMPLEContractAddress1234567890

# Disable/hide swap links (makes them informational only)
NEXT_PUBLIC_DISABLE_SWAP_LINKS=true

# ===================================================================
# Rate Limiting Configuration (API Protection)
# ===================================================================

# Sliding window duration in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# Maximum requests allowed per window (default: 10)
RATE_LIMIT_MAX_REQS=10

# Token bucket capacity - max burst requests (default: 5)
RATE_LIMIT_BURST=5

# Token refill interval in milliseconds (default: 5000 = 5 seconds)
RATE_LIMIT_REFILL_MS=5000

# Tokens added per refill interval (default: 1)
RATE_LIMIT_REFILL_TOKENS=1

# ===================================================================
# Upstash Redis (for Serverless environments - Vercel/Cloudflare)
# ===================================================================
# Get these from https://console.upstash.com/
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here

# ===================================================================
# CORS Configuration
# ===================================================================
# Comma-separated list of allowed origins (production domains)
# Leave empty for development (allows all origins)
CORS_ALLOWED_ORIGINS=https://fromzero.app,https://www.fromzero.app,http://localhost:3030

# ===================================================================
# Payload Protection
# ===================================================================
# Maximum payload size in bytes (default: 4096 = 4KB)
PAYLOAD_MAX_BYTES=4096

# ===================================================================
# How to use:
# ===================================================================
#
# 1. NEXT_PUBLIC_COMMUNITY_ONLY
#    - true: Shows community page link in header
#    - false: Hides community page link
#
# 2. NEXT_PUBLIC_FZ_TOKEN_CONTRACT
#    - Set to actual Solana contract address
#    - Used in ContractBlock component
#
# 3. NEXT_PUBLIC_DISABLE_SWAP_LINKS
#    - true: Hides swap/trade buttons (shows "informational only")
#    - false: Shows swap buttons (if community features enabled)
#
# ===================================================================
# Example usage in components:
# ===================================================================
#
# const COMMUNITY_MODE = process.env.NEXT_PUBLIC_COMMUNITY_ONLY === "true";
# const DISABLE_SWAP = process.env.NEXT_PUBLIC_DISABLE_SWAP_LINKS === "true";
#
# {COMMUNITY_MODE && <Link href="/community">FZ Token</Link>}
# {!DISABLE_SWAP && <Button>Swap</Button>}
#
# ===================================================================
