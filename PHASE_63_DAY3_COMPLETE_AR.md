# المرحلة 63 - اليوم الثالث: توليد التقارير اليومية - مكتمل ✅

**الحالة**: ✅ **التنفيذ مكتمل** - جاهز للنشر والاختبار
**التاريخ**: 2025-11-07

---

## نظرة عامة

تم بنجاح تنفيذ نظام توليد تقارير يومية تلقائي مع:
- **توليد PDF و XLSX مجدول** من المقاييس اليومية
- **تكامل Cloud Storage** للاستضافة الآمنة للملفات
- **توليد روابط موقّعة** للوصول المؤقت للتنزيل
- **إمكانية إعادة** للتقارير التاريخية (1-60 يوم)
- **لوحة UI** متكاملة في لوحة التحليلات
- **التحكم في الوصول للمدراء فقط** لعمليات الإعادة

---

## ما تم تنفيذه

### 1. دوال Cloud Functions لتوليد التقارير ✅
**الملف**: [functions/src/reports/generateDailyReport.ts](functions/src/reports/generateDailyReport.ts:1)

**الميزات**:
- ✅ **توليد PDF** باستخدام pdf-lib
  - تخطيط احترافي مع علامة From Zero Labs التجارية
  - قسم الملخص (الإجماليات، نسبة الأخطاء، مقاييس التأخير)
  - أفضل أنواع الأحداث (أفضل 8)
  - أفضل الاستراتيجيات (أفضل 8)
  - تذييل مع طابع زمني للتوليد

- ✅ **توليد XLSX** باستخدام ExcelJS
  - **الورقة 1 (KPIs)**: جميع المقاييس الرئيسية مع رؤوس منسقة
  - **الورقة 2 (أنواع الأحداث)**: تفصيل كامل مرتب حسب العدد
  - **الورقة 3 (الاستراتيجيات)**: تفصيل كامل مرتب حسب العدد
  - تنسيق احترافي مع ألوان الرؤوس

- ✅ **رفع إلى Cloud Storage**
  - بنية مسار منظمة: `reports/daily/{date}/report-{date}.pdf`
  - ملفات خاصة (غير قابلة للوصول العام)
  - رؤوس التحكم في التخزين المؤقت للأداء
  - تتبع البيانات الوصفية (الحجم، نوع المحتوى)

- ✅ **بيانات Firestore الوصفية**
  - تخزين مسارات الملفات والبيانات الوصفية في `ops_reports/{date}`
  - يستخدمها API لتوليد روابط التنزيل الموقعة

**الدوال المصدّرة**:
```typescript
export const generateDailyReport            // مجدولة (02:20 توقيت الكويت)
export const generateDailyReportBackfill   // قابلة للاستدعاء (للمدراء فقط)
```

---

### 2. الدالة المجدولة ✅

تعمل يوميًا في **02:20 توقيت الكويت** (10 دقائق بعد تجميع المقاييس):

```typescript
الجدول: "20 2 * * *"
المنطقة الزمنية: "Asia/Kuwait"
الذاكرة: 512MiB
المهلة: 120 ثانية
```

**العملية**:
1. حساب تاريخ الأمس (اليوم الكامل السابق)
2. جلب المقاييس من `ops_metrics_daily`
3. توليد تقرير PDF
4. توليد تقرير XLSX
5. رفع كليهما إلى Cloud Storage
6. كتابة البيانات الوصفية إلى مجموعة `ops_reports`

**السجلات**:
```
📊 Generating report for 2025-11-06...
  → Generating PDF...
  → Generating XLSX...
  → Uploading to Storage...
  → Writing metadata to Firestore...
✅ Report generated for 2025-11-06: PDF (45KB), XLSX (12KB)
```

---

### 3. دالة الإعادة القابلة للاستدعاء ✅

**الغرض**: إعادة توليد التقارير للتواريخ التاريخية

**الوصول**: للمدراء فقط (يتطلب custom claim `admin: true`)

**المعاملات**:
- `days` (رقم): عدد الأيام للإعادة (1-60، افتراضي 7)

**الاستخدام**:
```typescript
import { getFunctions, httpsCallable } from 'firebase/functions';

const functions = getFunctions();
const backfill = httpsCallable(functions, 'generateDailyReportBackfill');

// إعادة توليد آخر 7 أيام
const result = await backfill({ days: 7 });
console.log(result.data);
// {
//   success: true,
//   processed: 7,
//   results: [
//     { date: "2025-11-06", pdf: {...}, xlsx: {...} },
//     { date: "2025-11-05", pdf: {...}, xlsx: {...} },
//     ...
//   ]
// }
```

---

### 4. تحديث قواعد Firestore ✅
**الملف**: [firestore.rules](firestore.rules:841-854)

إضافة قواعد لمجموعة `ops_reports`:

```javascript
// التقارير اليومية - بيانات وصفية لملفات PDF و XLSX
match /ops_reports/{date} {
  // أي مستخدم مصادق عليه يمكنه قراءة البيانات الوصفية للتقرير
  // (الملفات الفعلية في Cloud Storage مع روابط موقعة)
  allow read: if isSignedIn();

  // المدراء فقط يمكنهم الكتابة (عبر Cloud Functions)
  allow create, update: if isAdmin();
  allow delete: if false; // لا حذف أبدًا للتقارير
}
```

---

### 5. مسار API للتقارير ✅
**الملف**: [src/app/api/ops/reports/route.ts](src/app/api/ops/reports/route.ts:1)

**نقطة النهاية**: `GET /api/ops/reports`

**الميزات**:
- ✅ مصادقة Firebase مطلوبة
- ✅ جلب آخر 14 تقرير من Firestore
- ✅ توليد روابط موقعة لكل ملف (صالحة 15 دقيقة)
- ✅ رؤوس التحكم في التخزين المؤقت (دقيقة واحدة cache، دقيقتان stale-while-revalidate)

**الاستجابة**:
```json
{
  "items": [
    {
      "date": "2025-11-06",
      "createdAt": 1730930400000,
      "pdf": {
        "path": "reports/daily/2025-11-06/report-2025-11-06.pdf",
        "size": 45678,
        "contentType": "application/pdf",
        "url": "https://storage.googleapis.com/..."
      },
      "xlsx": {
        "path": "reports/daily/2025-11-06/report-2025-11-06.xlsx",
        "size": 12345,
        "contentType": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        "url": "https://storage.googleapis.com/..."
      }
    },
    ...
  ]
}
```

---

### 6. مكون ReportsPanel للواجهة ✅
**الملف**: [src/features/ops/analytics/ReportsPanel.tsx](src/features/ops/analytics/ReportsPanel.tsx:1)

**الميزات**:
- ✅ جلب التقارير من API
- ✅ عرض آخر 14 تقرير في قائمة قابلة للتمرير
- ✅ أزرار تنزيل لـ PDF و XLSX
- ✅ عرض حجم الملف (منسق: KB/MB)
- ✅ حالات التحميل والخطأ
- ✅ دعم ثنائي اللغة (ar/en)
- ✅ تصميم متجاوب

**عناصر الواجهة**:
```
┌──────────────────────────────────────┐
│  التقارير اليومية       [14 تقرير] │
├──────────────────────────────────────┤
│  2025-11-06                          │
│  PDF: 45KB | XLSX: 12KB              │
│  [تحميل PDF] [تحميل XLSX]           │
├──────────────────────────────────────┤
│  2025-11-05                          │
│  PDF: 43KB | XLSX: 11KB              │
│  [تحميل PDF] [تحميل XLSX]           │
└──────────────────────────────────────┘
```

---

### 7. التكامل مع لوحة التحليلات ✅
**الملف**: [src/features/ops/analytics/AnalyticsPage.tsx](src/features/ops/analytics/AnalyticsPage.tsx:156)

إضافة ReportsPanel أسفل رسم اتجاه المقاييس:

```tsx
{/* لوحة التقارير اليومية */}
<ReportsPanel locale={locale} />
```

**تخطيط لوحة التحكم**:
```
┌─────────────────────────────────────────┐
│  لوحة تحليلات العمليات  [النطاق: 7د]  │
├─────────────────────────────────────────┤
│  [الإجمالي] [نسبة الأخطاء] [متوسط]    │ ← بطاقات KPI
├─────────────────────────────────────────┤
│  [رسم اتجاه المقاييس - Recharts]      │ ← رسم
├─────────────────────────────────────────┤
│  [لوحة التقارير اليومية]              │ ← جديد!
│  • 2025-11-06 [PDF] [XLSX]              │
│  • 2025-11-05 [PDF] [XLSX]              │
└─────────────────────────────────────────┘
```

---

## التبعيات المضافة

**functions/package.json**:
```json
{
  "dependencies": {
    "exceljs": "^4.4.0",
    "pdf-lib": "^1.17.1"
  }
}
```

**لماذا هذه المكتبات**:
- **exceljs**: خفيفة الوزن، غنية بالميزات لتوليد Excel (بدون تبعيات ثنائية)
- **pdf-lib**: توليد PDF بلغة JavaScript النقية (بدون Puppeteer، حجم حزمة أصغر)

---

## تدفق البيانات

```
┌──────────────────────────────────────────────────────────┐
│  مشغل مجدول (02:20 توقيت الكويت يوميًا)                 │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ↓
         ┌──────────────────────┐
         │ generateDailyReport  │
         └────────┬─────────────┘
                  │
                  ├→ 1. جلب من ops_metrics_daily/{date}
                  │
                  ├→ 2. توليد PDF Buffer (pdf-lib)
                  │    • رأس مع العلامة التجارية
                  │    • الملخص (KPIs)
                  │    • أفضل أنواع الأحداث
                  │    • أفضل الاستراتيجيات
                  │
                  ├→ 3. توليد XLSX Buffer (ExcelJS)
                  │    • الورقة 1: KPIs
                  │    • الورقة 2: أنواع الأحداث
                  │    • الورقة 3: الاستراتيجيات
                  │
                  ├→ 4. رفع إلى Cloud Storage
                  │    reports/daily/{date}/report-{date}.pdf
                  │    reports/daily/{date}/report-{date}.xlsx
                  │
                  └→ 5. كتابة البيانات الوصفية إلى Firestore
                       ops_reports/{date}
                       { date, files: { pdf, xlsx }, createdAt }
```

---

## دليل الاختبار

### 1. الاختبار المحلي مع المحاكيات

#### المتطلبات الأساسية
```bash
# تأكد من اكتمال اليوم الأول (تجميع المقاييس)
# تأكد من وجود بيانات في ops_metrics_daily ليوم واحد على الأقل
```

#### بدء المحاكيات
```bash
firebase emulators:start --only functions,firestore,storage
```

#### اختبار دالة الإعادة
```bash
firebase functions:shell

# في الشل:
> generateDailyReportBackfill({ days: 1 })
```

الإخراج المتوقع:
```javascript
{
  success: true,
  processed: 1,
  results: [
    {
      date: "2025-11-06",
      pdf: { path: "reports/daily/...", size: 45678, ... },
      xlsx: { path: "reports/daily/...", size: 12345, ... }
    }
  ]
}
```

#### التحقق من الملفات في Storage
```bash
# تحقق من محاكي Cloud Storage
# افتراضي: http://localhost:9199
```

#### التحقق من مستندات Firestore
```bash
firebase firestore:get ops_reports/2025-11-06
```

---

### 2. الاختبار في الإنتاج

#### نشر الدوال
```bash
# بناء الدوال
cd functions && pnpm build

# نشر دوال التقارير
firebase deploy --only functions:generateDailyReport,functions:generateDailyReportBackfill

# نشر قواعد Firestore
firebase deploy --only firestore:rules
```

#### اختبار الإعادة (وحدة تحكم الويب)
```typescript
import { getFunctions, httpsCallable } from 'firebase/functions';

const functions = getFunctions();
const backfill = httpsCallable(functions, 'generateDailyReportBackfill');

// يجب أن يكون المستخدم مدير
try {
  const result = await backfill({ days: 7 });
  console.log('✅ الإعادة ناجحة:', result.data);
} catch (error) {
  console.error('❌ فشلت الإعادة:', error);
}
```

#### عرض التقارير في لوحة التحكم
```
انتقل إلى: https://your-app.web.app/ar/ops/analytics
أو: http://localhost:3000/ar/ops/analytics
```

المتوقع:
- ✅ لوحة التقارير مرئية أسفل الرسم
- ✅ قائمة التقارير الأخيرة مع التواريخ
- ✅ أزرار تنزيل PDF و XLSX
- ✅ تنزيل الملفات بنجاح عند النقر

---

## بنية تقرير PDF

```
┌─────────────────────────────────────────┐
│  From Zero Labs — تقرير يومي للعمليات  │
│  التاريخ (UTC): 2025-11-06              │
│                                          │
│  الملخص                                  │
│  إجمالي الأحداث: 1,523                  │
│  معلومات: 1,205   تحذير: 245   خطأ: 73 │
│  نسبة الأخطاء: 4.8%                     │
│                                          │
│  مقاييس التأخير                          │
│  المتوسط: 156 مللي ثانية                │
│  p50 (الوسيط): 142 مللي ثانية           │
│  p95 (النسبة المئوية 95): 387 مللي ثانية│
│                                          │
│  أفضل أنواع الأحداث                     │
│  • ingest: 456                           │
│  • normalize: 389                        │
│  • rag.validate: 234                     │
│  • export: 221                           │
│  ...                                     │
│                                          │
│  أفضل الاستراتيجيات                     │
│  • default: 892                          │
│  • fast: 431                             │
│  • safe: 200                             │
│  ...                                     │
│                                          │
│  تم التوليد في: 2025-11-07T02:20:15.234Z│
│  From Zero Labs - تحليلات العمليات      │
└─────────────────────────────────────────┘
```

---

## الأمان

### 1. التحكم في الوصول
- **قواعد Firestore**: المستخدمون المصادق عليهم فقط يمكنهم قراءة بيانات التقرير الوصفية
- **Cloud Storage**: الملفات خاصة (غير قابلة للوصول العام)
- **الروابط الموقعة**: وصول مؤقت (15 دقيقة انتهاء الصلاحية)
- **دالة الإعادة**: للمدراء فقط (فحص custom claim)

### 2. أمان Cloud Storage
الملفات المخزنة مع:
- ACL خاص (غير عام)
- بيانات وصفية مخصصة
- رؤوس التحكم في التخزين المؤقت
- لا يوجد وصول URL مباشر بدون توقيع

### 3. مصادقة API
- التحقق من رمز ID مطلوب
- روابط موقعة قصيرة العمر (15 دقيقة)
- تحديد المعدل عبر التحكم في التخزين المؤقت

---

## الخطوات التالية

### فوري (مطلوب)
1. ✅ جميع الملفات تم إنشاؤها واختبارها
2. 🔄 نشر الدوال: `firebase deploy --only functions:generateDailyReport,functions:generateDailyReportBackfill`
3. 🔄 نشر قواعد Firestore: `firebase deploy --only firestore:rules`
4. 🔄 اختبار الإعادة مع مستخدم مدير
5. 🔄 التحقق من تشغيل الدالة المجدولة في 02:20 توقيت الكويت

### اختياري (تحسينات)
1. إضافة إشعارات بريد إلكتروني عندما تكون التقارير جاهزة
2. دعم نطاقات تاريخ مخصصة للتقارير
3. إضافة قوالب تقارير (ملخصات أسبوعية، شهرية)
4. تضمين رسوم بيانية/جداول في تقارير PDF
5. إضافة خيار تنزيل ZIP (كلا الملفين معًا)
6. تنفيذ انتهاء صلاحية التقرير/التنظيف (على سبيل المثال، الاحتفاظ بآخر 90 يومًا)

---

## الخلاصة

✅ **المرحلة 63 - اليوم الثالث مكتمل**

تم بنجاح تنفيذ نظام توليد تقارير يومية تلقائي مع:
- توليد PDF و XLSX مجدول (02:20 توقيت الكويت يوميًا)
- تكامل Cloud Storage مع روابط موقعة
- إعادة للمدراء فقط للتقارير التاريخية (1-60 يوم)
- لوحة UI في لوحة التحليلات
- قواعد أمان وتحكم وصول كاملة
- تنسيق تقرير احترافي
- دعم ثنائي اللغة (ar/en)

**جاهز للنشر والاستخدام في الإنتاج!**

---

**التاريخ**: 2025-11-07
**وقت التنفيذ**: ~ساعتان
**الحالة**: ✅ مكتمل
**البناء**: ✅ ناجح
**التبعيات**: المرحلة 63 الأيام 1 و 2 (تجميع المقاييس + لوحة التحليلات)
