'use client';
import React from 'react';

interface LegalReportButtonProps {
  dsarId: string;
}

export default function LegalReportButton({ dsarId }: LegalReportButtonProps) {
  return <button style={{opacity:.6}}>LegalReportButton (stub)</button>;
}
